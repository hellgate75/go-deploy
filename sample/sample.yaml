name: Deploy sample commands
steps:
- name: Run shell command and save it in a folder
  shell: 
    exec: ls -latr
    saveState: list_folder
- name: Print list_folder variable
  shell:
    exec: echo -e "{{ list_folder }}"
    withVars:
    - list_folder